# Server only (used by the Dockerfile). The launcher's dependencies are in requirements.txt.
Flask
Flask-Limiter
flask_httpauth
python-dotenv
gunicorn>=26  # the Dockerfile uses --no-control-socket (added in 26)
